[pytest]
testpaths = tests
addopts = -q --disable-warnings --alluredir=artifacts/allure-results
markers =
    smoke: fast, PR-gating suite
    sanity: nightly core suite
    regression: extended suite


